fastapi
uvicorn
pandas==1.5.3  # Older, more stable version
python-multipart
numpy==1.23.5  # Stable version for older pandas/scikit-learn
# You may need to add scikit-learn here if you use it in your code, 
# but we are skipping it since your API is mock-based.
